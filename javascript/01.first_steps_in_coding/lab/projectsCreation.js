function projectsCreation(name, projects) {
    project = 3;

    totalHours = project * projects;

    console.log ("The architect " + name + " will need " + totalHours + " hours to complete " + projects + " project/s.");
}

projectsCreation("Geogrge", 4);
